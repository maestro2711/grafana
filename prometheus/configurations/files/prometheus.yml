scrape_configs:
  - job_name: prometheus
    # The URL of the Prometheus server to scrape metrics from
    metrics_path: /metrics
    static_configs:
      - targets: 
          - host.docker.internal:9090
        labels:
          instance: prometheus